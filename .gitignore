# OS
.DS_Store
Thumbs.db

# Editors
.idea/
.vscode/
*.swp
*.swo
*~

# Local tooling (this repo is Markdown-only — no app build)
node_modules/
.env
.env.*
dist/
.cache/

# Secrets — never commit
*.pem
*.key
credentials.json
